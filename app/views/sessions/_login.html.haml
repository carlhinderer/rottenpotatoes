#login
  - if @current_user
    %p.welcome Logged in as #{@current_user.name}
    = link_to 'Log out', logout_path
  - else
    %p.login= link_to 'Log in with Twitter account', login_path, :provider => 'twitter'
